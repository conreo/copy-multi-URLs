{
  "name": "copy-multi-urls",
  "version": "1.0.1",
  "private": true,
  "description": "Firefox extension to copy all URLs from selected text",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "web-ext lint --ignore-files='tests/**' 'node_modules/**' 'vitest.config.js' 'package.json' 'package-lock.json'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.8",
    "web-ext": "^10.3.0"
  }
}
