{
  "name": "copy-multi-urls",
  "version": "1.0.0",
  "private": true,
  "description": "Firefox extension to copy all URLs from selected text",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "web-ext lint --ignore-files='tests/**' 'node_modules/**' 'vitest.config.js' '*.json'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.0",
    "jsdom": "^26.0.0",
    "vitest": "^3.1.0",
    "web-ext": "^10.3.0"
  }
}
